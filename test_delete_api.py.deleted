import requests
import json

# 削除APIをテスト
api_url = "http://localhost:7999/api/car-settings/3/"

print("🧪 愛車設定削除APIテスト開始")
print(f"URL: {api_url}")

try:
    response = requests.delete(api_url)
    print(f"ステータスコード: {response.status_code}")
    print(f"レスポンス: {response.text}")
    
    if response.status_code == 200:
        print("✅ 削除成功")
    else:
        print("❌ 削除失敗")
        
except Exception as e:
    print(f"❌ エラー: {e}")
